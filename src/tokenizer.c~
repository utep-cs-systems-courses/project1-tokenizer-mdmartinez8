#include <stdio.h>
#include <stdlib.h>

int main()
{
  char *buffOut = (char *)malloc(1000*sizeof(char));
  char *buffIn = (char *)malloc(1000*sizeof(char));
  char* printWords(char *buffIn);
  //char *buffInP = &buffIn[0];
  //char *buffOutP = &buffOut[0];
  do
  {
    printf("$");
    gets(buffIn);
  }while((*buffOut++ = *buffIn++)) != '\0');

  printf("\n");

  /*char* printWords(char *buffIn)
  {
    char *buffOut = (char*)malloc(1000*sizeof(char));
    while((*buffOut = *buffIn) != '\0')
    {
      putchar(buffIn);
      buffIn++;
    }
    return buffOut;

    }**/
  return 0;
}  
